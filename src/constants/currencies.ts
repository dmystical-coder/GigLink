import { Token } from '@/types/bounty';

export const SUPPORTED_TOKENS: Token[] = ['ETH', 'USDC', 'DEGEN'];

export const CURRENCY_LOGOS: Record<Token, string> = {
  ETH: '/icons/eth.svg', // Placeholder paths
  USDC: '/icons/usdc.svg',
  DEGEN: '/icons/degen.svg',
};
